<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.lucash.scorevolley">

    <application
        android:allowBackup="true"
        android:fullBackupOnly="true"
        android:icon="@drawable/tableau_de_score"
        android:label="@string/app_name"
        android:roundIcon="@drawable/tableau_de_score"
        android:supportsRtl="true"
        android:theme="@style/Theme.ScoreVolley">

        <activity
            android:name=".controller.Match_activity"
            android:exported="false" />
        <activity
            android:name=".controller.Team_creation_activity"
            android:exported="false" />
        <activity
            android:name=".controller.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>